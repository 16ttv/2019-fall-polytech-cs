def planets(a, b, c):
	assert a > 0
	assert b > 0
	assert c > 0
	def algorithm(x, y):
		while a != y:
			if x > y:
				x = x - y
			else:
				y = y - x
		return y
	print((((a * b) / algorithm(a, b) * l) / algorithm(b, c)))
	
planets(36, 12, 24)